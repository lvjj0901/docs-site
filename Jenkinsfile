//jenkins pipeline
pipeline{
    agent any

    stages{
        stage("pull code"){
            steps{
                echo "pulling..."
                sh "ls -la"
            }
        }
        stage("build project"){
            steps{
                echo "building..."
                sh "ls -ls"
            }
        }
    }
}